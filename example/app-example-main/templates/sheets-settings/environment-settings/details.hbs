<section
    class='tab {{tabs.details.cssClass}} {{tabs.details.id}}'
    data-tab='{{tabs.details.id}}'
    data-group='{{tabs.details.group}}'
>
    <fieldset class="one-column">
        <legend>{{localize 'APP_EXAMPLE.GENERAL.basics'}}</legend>
        <div class="nest-inputs">
            {{formGroup systemFields.tier value=document._source.system.tier localize=true}}
            {{formGroup systemFields.type value=document._source.system.type localize=true}}
            {{formGroup systemFields.difficulty value=document._source.system.difficulty localize=true}}
        </div>
        {{formField systemFields.description value=document._source.system.description label=(localize "APP_EXAMPLE.ACTORS.Environment.FIELDS.description.label")}}
        {{formField systemFields.impulses value=document._source.system.impulses label=(localize "APP_EXAMPLE.ACTORS.Environment.FIELDS.impulses.label")}}
    </fieldset>
</section>