<ul class="app-example chat damage-summary">
    {{#each targets}}
        <li class="token-target-container {{#if this.token.id}}clickable{{/if}}" data-token="{{this.token.id}}">
            <header>
                <img src="{{this.token.texture.src}}" />
                <h2 class="actor-name">{{this.token.name}}</h2>
            </header>
            <ul class="damage-container">
                {{#each this.updates}}
                    <li class="damage-row">
                        {{#if (gte this.value 0)}}
                            <span>
                                {{
                                localize "APP_EXAMPLE.UI.Chat.markResource"
                                quantity=this.value
                                resource=(localize (concat "APP_EXAMPLE.CONFIG.HealingType." this.key ".name"))
                                }}
                            </span>
                        {{else}}
                            <span>
                                {{
                                localize "APP_EXAMPLE.UI.Chat.clearResource"
                                quantity=(positive this.value)
                                resource=(localize (concat "APP_EXAMPLE.CONFIG.HealingType." this.key ".name"))
                                }}
                            </span>
                        {{/if}}
                    </li>
                {{/each}}
            </ul>
        </li>    
    {{/each}}
</ul>