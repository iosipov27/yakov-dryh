<div class="app-example dh-style tooltip card-style">
    <img class="tooltip-image" src="{{item.img}}" />
    <h2 class="tooltip-title">{{localize item.name}}</h2>
    <div class="tags">
        {{#if item.uses.max}}
            <div class="tag">
              <span>{{localize "APP_EXAMPLE.GENERAL.used"}} {{item.uses.value}}</span>
            </div>
            <div class="tag">
              <span>{{localize "APP_EXAMPLE.GENERAL.max"}} {{formulaValue item.uses.max item}}</span>
            </div>
            <div class="tag">
              <span>{{localize "APP_EXAMPLE.GENERAL.recovery"}} {{#with (lookup config.GENERAL.refreshTypes item.uses.recovery) as | type |}}{{localize type.label}}{{/with}}</span>
            </div>
        {{/if}}
        {{#if (gt item.cost.length 0)}}
            {{#each item.cost as | cost |}}
                <div class="tag">
                  <span>{{localize "Type"}} {{#with (lookup @root.config.GENERAL.abilityCosts cost.type) as | type |}}{{localize type.label}}{{/with}}</span>
                </div>
                <div class="tag">
                  <span>{{localize "APP_EXAMPLE.GENERAL.value"}} {{cost.value}}</span>
                </div>
                {{#if cost.scalable}}
                  <div class="tag">
                    <span>{{localize "APP_EXAMPLE.GENERAL.scalable"}} {{localize "APP_EXAMPLE.GENERAL.true"}}</span>
                  </div>
                  <div class="tag">
                    <span>{{localize "APP_EXAMPLE.GENERAL.step"}} {{cost.step}}</span>
                  </div>
                {{/if}}
            {{/each}}
        {{/if}}
        {{#if (or item.range item.target)}}
            <div class="tag">
              <span>
                  {{localize "APP_EXAMPLE.GENERAL.range"}} 
                  {{#if item.range}}
                      {{#with (lookup @root.config.GENERAL.range item.range) as | range |}}
                          {{localize range.label}}
                      {{/with}}
                  {{else}}
                      {{localize "APP_EXAMPLE.GENERAL.none"}}
                  {{/if}}
              </span>
            </div>
        {{/if}}
        <div class="tag">
          <span>
              {{localize "APP_EXAMPLE.GENERAL.Target.single"}} 
              {{#if item.target.type}}
                  {{#with (lookup @root.config.ACTIONS.targetTypes item.target.type) as | target |}}
                      {{@root.item.target.amount}} {{localize target.label}}
                  {{/with}}
              {{else}}
                  {{localize "APP_EXAMPLE.GENERAL.none"}}
              {{/if}}
          </span>
        </div>
    </div>
    {{#if description}}
        <div class="tooltip-description">{{{description}}}</div>
    {{/if}}
    <p class="tooltip-hint">
        <i class="fa-solid fa-computer-mouse"></i> {{localize "APP_EXAMPLE.UI.Tooltip.middleClick"}}
    </p>
</div>